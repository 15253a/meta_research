interface:
  display_name: "Implement Ticket"
  short_description: "Implement one frozen ticket in an isolated branch"
  default_prompt: "Use $implement-ticket to implement the frozen ticket snapshot, test it, review it, and commit it."
policy:
  allow_implicit_invocation: false
