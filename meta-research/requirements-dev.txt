# 自验测试依赖（M0）。运行：python -m pip install -r requirements-dev.txt
# 本机注：pip 走 ivolces 镜像源须不带代理（与 codex 需代理相反）。
pytest>=7
jsonschema>=4.18   # draft 2020-12 校验（$ref 注册表 M1 起用 referencing.Registry）
PyYAML>=6
