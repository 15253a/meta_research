# Restricted, revocable automation key used by Codex inside the sandbox.
# Stock Sandcastle 0.12.0 exposes it to the whole container lifetime, so use
# this environment only with trusted repository code.
CODEX_API_KEY=
