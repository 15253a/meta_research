{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://meta-research.local/schemas/interaction_reply_candidate.schema.json",
  "title": "interaction_reply_candidate",
  "description": "只读 Codex 讲解员的最小候选回复。只返回面向用户的自然语言 answer；快照绑定与 grounding 由编排器单独入库和执行，不要把内部溯源字段塞进用户回复契约。",
  "type": "object",
  "required": ["answer"],
  "additionalProperties": false,
  "properties": {
    "answer": {
      "type": "string",
      "minLength": 1,
      "maxLength": 3000,
      "description": "面向用户的简洁自然语言回答；先说结论，默认翻译而不展示内部术语。不得声称状态变更、引用日志作科学证据、生成 directive 或扩写上下文外的运行事实。"
    }
  }
}
