# meta-research 元循环系统 · 全套流程图（v2.2）

> 依据《meta-research 元循环系统 · 施工说明书 v2.2》（`../施工说明书-v2.2.md`）绘制。
> 图中术语（实体名、状态值、门禁名、产物文件名）与主文档/DDL 逐字一致，可直接对照施工。
> 元循环顺序 = **idea → plan → bundle → reasoning**（reasoning 轮尾、首轮特殊、无独立 closing；dependency_wait 轮由 plan 机械收尾，§4.2.5）；对照元 = **evaluation**；variant 不连 protocol。

**阅读顺序建议**：图 00（系统长什么样）→ 图 01（一轮怎么转）→ 图 02–05（四阶段内部细节）→ 图 06（实体状态机 06a–06g · 对象关系 06h · 五张机制图 06i–06m）→ 图 07–09（人类介入、上下文供给、长时运行）。

| 图号 | 内容 | 对应章节 |
|------|------|------------|
| 00 | 系统总览：四层架构 + 元循环骨架（00a/00b） | §2 §3 |
| 01 | 一轮主循环 advance(cycle) 全景 | §3 §8.1 |
| 02 | Reasoning（轮尾）：答题 · 树维护 · 路由 · 收尾 | §3 §7 |
| 03 | Idea：发散 · 统一审计 · 选定 | §7 |
| 04 | Plan：复用四情形 · 锁评估协议 | §4 §7.2 |
| 05 | Bundle：串行构建 · 训练 · 评估 · 注册 | §7 §9 |
| 06 | 实体状态机（06a–06g + 06e2 attempt）· 对象关系（06h）· 机制图（06i–06m） | §4 §9 §10 §12 |
| 07 | 人类审计与介入（web + QQ + 自然语言） | §13 |
| 08 | 上下文编译与数据写回闭环（含 validate_artifact 预检环） | §5 §11 |
| 09 | 长时运行：恢复 · watchdog · 预算 · 停机 | §6 §8 |

**图例（全套统一配色）**：

<!-- png: legend -->
```mermaid
flowchart LR
    L1["流程 / 阶段"]:::phase --- L2["共享状态 / 资产"]:::asset --- L3["人类相关"]:::human --- L4["门禁 / 校验 / 独立审计"]:::gate --- L5["失败路径"]:::fail --- L6(["终止 / 停机"]):::term --- L7{"判断"}:::dec --- L8["编排器 / 运行时（确定性）"]:::orch
    classDef phase fill:#dbeafe,stroke:#1d4ed8,color:#1e3a8a
    classDef asset fill:#dcfce7,stroke:#15803d,color:#14532d
    classDef human fill:#ffedd5,stroke:#c2410c,color:#7c2d12
    classDef gate fill:#ede9fe,stroke:#6d28d9,color:#4c1d95
    classDef fail fill:#fee2e2,stroke:#b91c1c,color:#7f1d1d
    classDef term fill:#e5e7eb,stroke:#4b5563,color:#1f2937
    classDef dec fill:#fef9c3,stroke:#a16207,color:#713f12
    classDef orch fill:#f1f5f9,stroke:#475569,color:#0f172a
```

边语义：**实线 = 主流程**；**虚线 = 对共享状态的读写 / 旁路信息流**；边上文字 = 触发条件或失败语义。

---

## 图 00 · 系统总览：四层架构 + 元循环骨架（§2 §3）

**这张图回答**：系统由哪几层组成、智能与确定性如何分工。**00a 元循环总览**（主环 idea→plan→bundle→reasoning + 共享状态 + 人类指令 + 终止出口）；**00b 四层视图**。核心架构决策：**编排器是确定性程序、从不推理；Codex 会话是无状态工人、从不持有状态**。

<!-- png: 00a-元循环总览 -->
```mermaid
flowchart TD
    HUMAN["人类指令（directive）<br/>轮始消费（P5：经收件箱，由门禁消费，不直接改库）"]:::human
    TERM(["终止：三判据任一满足<br/>① 全 open 问题低分连续若干轮 ② 全局预算耗尽 ③ 目标谓词满足"]):::term

    I["Idea<br/>发散 · 审计 · 选定"]:::phase
    P["Plan<br/>复用判定 · 锁评估协议"]:::phase
    B["Bundle<br/>串行构建 · 训练 · 评估"]:::phase
    R["Reasoning（轮尾）<br/>答本轮 · 出下题 · 路由 · 收尾<br/>（cycle_report / views / Git / 记账 / 通知）"]:::phase

    I --> P --> B --> R
    R -- "攻坚：选定下一题，开下一轮" --> I
    R -- "分解：写子问题，下轮攻坚子问题" --> I
    R -. "首轮特殊：reasoning 先出首题（无前置三阶段）" .-> I
    R -- "终止：三判据任一满足，收尾即止" --> TERM

    HUMAN -. "轮始消费：注题 / 调权 / 剪枝 / 改目标" .-> R
    B -. "文件请求（确实无法自行获取 → 聚合请求单 · 全局等待，§4.6.8）" .-> HUMAN

    subgraph SHARED["共享状态（SQLite = 唯一真相，md 仅为渲染视图；写入只经门禁(不变量校验)）"]
        direction LR
        TREE["问题树<br/>状态：答案与树结构写回"]:::asset
        POOL["baseline 池<br/>资产：baseline / variant / checkpoint / evaluation"]:::asset
        RR["run + evaluation 记录<br/>训练执行与评估测量（成败同记）"]:::asset
    end
    R -. "答案写回 · 上下文读取" .-> TREE
    P -. "复用判定 · gate_claim 占坑" .-> POOL
    B -. "run + checkpoint + evaluation 入库 · gate_register 注册" .-> RR

    classDef phase fill:#dbeafe,stroke:#1d4ed8,color:#1e3a8a
    classDef asset fill:#dcfce7,stroke:#15803d,color:#14532d
    classDef human fill:#ffedd5,stroke:#c2410c,color:#7c2d12
    classDef gate fill:#ede9fe,stroke:#6d28d9,color:#4c1d95
    classDef fail fill:#fee2e2,stroke:#b91c1c,color:#7f1d1d
    classDef term fill:#e5e7eb,stroke:#4b5563,color:#1f2937
    classDef dec fill:#fef9c3,stroke:#a16207,color:#713f12
    classDef orch fill:#f1f5f9,stroke:#475569,color:#0f172a
```

**00b 四层视图**——循环与共享状态分别落在流程层与资产层；六原则归属：P1→资产层，P2/P3→策略层与流程层边界，P5/P6→运行层：

<!-- png: 00b-四层架构 -->
```mermaid
flowchart TB
    LP["<b>策略层</b> · policies/policy.yaml（版本入 Git，每旋钮带默认值）<br/>全部可调旋钮：采集权重 w1/w2/w3/c · 单轮预算 B(t) · 分解阈值<br/>终止判据 τ · 审计节奏 K · 重试与超时上限（含双评审轮数）· 检索配方<br/>会话双模式 A/B · 树规模护栏 · 资源声明 · 文件请求 —— P2 机制与策略分离"]:::orch
    LF["<b>流程层</b> · prompts/ 版本化提示词 + orchestrator 阶段步进函数<br/>四个无状态阶段 idea→plan→bundle→reasoning 的契约：触发 / 读取 / Codex 任务 / 产物协议 / 门禁与写入 / 失败语义<br/>—— 阶段无状态：输入只来自上下文包；裁决/作答轮收尾并入 reasoning 轮尾（dependency_wait 为 plan 机械收尾）"]:::phase
    LA["<b>资产层</b> · db/research.sqlite（WAL）+ views/（Git 仓）<br/>唯一真相：对象模型（结构 baseline × 训练设置 variant × 评估协议 evaluation）· 状态机 · 不变量与门禁<br/>七类卡片与索引 · md 渲染视图与轮次档案 —— P1 真相只在一处"]:::asset
    LR2["<b>运行层</b> · orchestrator/（Python 守护进程或 cron 步进）<br/>编排器（确定性循环驱动，从不推理）· Codex Runner 适配器（无状态工人，运行时可替换）<br/>watchdog · 预算台账 · 人类控制台（directive 收件箱 · 文件请求）—— P5 / P6 落在此层"]:::orch
    LP ~~~ LF ~~~ LA ~~~ LR2
    classDef phase fill:#dbeafe,stroke:#1d4ed8,color:#1e3a8a
    classDef asset fill:#dcfce7,stroke:#15803d,color:#14532d
    classDef orch fill:#f1f5f9,stroke:#475569,color:#0f172a
```

---

## 图 01 · 一轮主循环 advance(cycle) 全景（§3 §8.1）

**这张图回答**：编排器每被唤醒一次，一轮如何从开启走到收尾。要点：**每步完成即落库，下一次 advance 从断点续起（P6）**；**reasoning 是裁决与作答路径的唯一收口与收尾**（无独立 closing；dependency_wait 由 plan 机械收尾，§4.2.5）；任何阶段失败都如实入账后汇入 reasoning 收尾（P4 失败即观测）。本图为主干；**路由细分（reuse_only / eval_only / goal_amend / dependency_wait）见图04（plan 落 route）与图06g（CYCLE 状态机）**。

<!-- png: 01-主循环 -->
```mermaid
flowchart TD
    WAKE["cron / 循环守护唤醒：advance(cycle)<br/>把轮次状态机向前推一格 → 落库 → 退出（§8.1）"]:::orch
    WAKE --> RESUME{"存在在途 CYCLE？"}:::dec
    RESUME -- "是：读轮次状态字段，从断点阶段续跑（P6 幂等）" --> CTX
    RESUME -- "否" --> NEWC["创建新 CYCLE（status=created）"]:::orch

    NEWC --> DIR["处理 DIRECTIVE 收件箱（§13）<br/>· 立即生效：pause / abort / set_budget<br/>· 标记『本轮 reasoning 必须消费』：goal_amend ·<br/>inject_question · reprioritize · prune_branch · note"]:::human
    DIR -- "pause / abort_cycle（硬指令）" --> HALT(["挂起 / 轮次置 aborted（问题回 open）"]):::term
    DIR --> CTX["渲染上下文包（编译器按阶段检索配方）<br/>包哈希 + 分区 manifest 写 DECISION，快照归档"]:::orch

    CTX --> FIRST{"reasoning-only 轮？<br/>(bootstrap/decompose/goal_amend)"}:::dec
    FIRST -- "是：本轮只跑 reasoning（出题 / 分解 / 改版适用性）" --> RSN
    FIRST -- "否（attack 轮）：本轮已有攻坚目标 Qn（上一轮 reasoning 选定）" --> IDEA

    IDEA["① Idea 会话 + 独立审计会话<br/>对 Qn 给出当前最优想法"]:::phase
    IDEA --> PLAN["② Plan 会话 + 独立评审会话<br/>复用判定 · 锁评估协议 · 占坑 · build_target 队列"]:::phase
    PLAN --> BUNDLE["③ Bundle 逐目标串行<br/>构建 → smoke → 代码适配评审 → 训练/评估 → 结果评审 → 校验入库 → 注册"]:::phase
    BUNDLE --> RSN

    IDEA -- "候选全不合格（Qn 置 inconclusive、visit+1）" --> RSN
    PLAN -- "可回答性评审两轮不过（Qn 置 inconclusive）" --> RSN
    PLAN -- "依赖等待 / import deferred" --> DWAIT(["dependency_wait 收尾：写 dep + 释放 Qn 回 open + mark_cycle_done（§4.2.5，不经 reasoning）"]):::term
    BUNDLE -- "关键目标失败 → 早退（带失败摘要）" --> RSN

    RSN["④ Reasoning（轮尾，裁决与作答唯一收口 §3 §7）<br/>答 Qn（verdict + 证据，I3）→ 树维护 → 四元打分路由出 Q(n+1)<br/>收尾：cycle_report.md → 增量渲染 views/ → Git 提交 → LEDGER 记账 → 通知钩子"]:::phase
    RSN --> ROUTE{"路由（selection.json）"}:::dec
    ROUTE -- "攻坚：下一题 est_cost ≤ B(t)" --> NEXTC(["落库；下次唤醒攻坚 Q(n+1)"]):::orch
    ROUTE -- "分解：最优问题超预算 → 写子问题，下轮攻坚子问题" --> NEXTC
    ROUTE -- "终止：三判据任一满足" --> STOP(["全局停机：问题树 + baseline 池 +<br/>视图与决策账即最终可复查资产"]):::term
    NEXTC --> WAKE

    RSN -- "reasoning 产物非法：按策略重试 ≤2 仍败" --> FAILC["轮次失败入 DECISION（如实入账，不自行兜底）<br/>等待下一轮或人工"]:::fail

    classDef phase fill:#dbeafe,stroke:#1d4ed8,color:#1e3a8a
    classDef asset fill:#dcfce7,stroke:#15803d,color:#14532d
    classDef human fill:#ffedd5,stroke:#c2410c,color:#7c2d12
    classDef gate fill:#ede9fe,stroke:#6d28d9,color:#4c1d95
    classDef fail fill:#fee2e2,stroke:#b91c1c,color:#7f1d1d
    classDef term fill:#e5e7eb,stroke:#4b5563,color:#1f2937
    classDef dec fill:#fef9c3,stroke:#a16207,color:#713f12
    classDef orch fill:#f1f5f9,stroke:#475569,color:#0f172a
```

---

## 图 02 · Reasoning（轮尾）：答题 · 树维护 · 路由 · 收尾（§3 §7）

**这张图回答**：唯一的「解释证据、下结论、收尾」收口阶段内部怎么走。关键时间结构：**问题 Qn 的答案在本轮轮尾的 reasoning 产生**（用本轮 bundle 证据）；执行阶段只生产证据。I3（证据强制）在 gate_close_question 机械执行。**收尾（原 closing）并入本阶段末尾**。

<!-- png: 02-Reasoning -->
```mermaid
flowchart TD
    IN["读取（上下文包·四区）：本轮问题卡 Qn + 目标当前版全文 · 问题树局部视图 + 子树摘要卡<br/>本轮 bundle 结果（runs + evaluation + metric_results + 失败摘要）· 待消费指令 · 策略快照<br/>召回：相似结论 / 可对比 evaluation"]:::orch

    IN --> FIRST{"reasoning-only 轮？<br/>(bootstrap/decompose/goal_amend)"}:::dec
    FIRST -- "是：跳过 R1 答题" --> RO["reasoning-only 轮处理（按 route 分支）：<br/>bootstrap=建首题；decompose=对 active_question 写子问题(派生边)+decompose_count++（+父 active→open 释放+逐子 dep）+选一子问题；<br/>goal_amend=消费 directive→建 goal 新版本→open 重打分→answer_applicability 批次"]:::phase
    RO --> R3
    FIRST -- "否（attack 轮）" --> R1["R1 答题：对 Qn 给出<br/>verdict + 结论正文 + 证据引用列表（证据指向 evaluation）"]:::phase

    R1 --> V{"verdict"}:::dec
    V -- "answered / refuted" --> GATE["gate_close_question（I3 证据强制）<br/>校验每条证据真实存在：<br/>evaluation 指标 / 文献 / 已关闭子问题答案 / 人类裁定"]:::gate
    GATE -- "通过：挂证据写回问题树，问题关闭<br/>（父问题靠子答案聚合关闭，价值回传）" --> R2
    GATE -- "校验不过：退回重答（≤2，附录 C artifact_parse）" --> R1
    GATE -- "重答仍不过" --> INC
    V -- "inconclusive（证据不足 / 本轮失败）" --> INC["问题置 inconclusive（非终态、可重调度）并入账<br/>visit_count + 1"]:::fail
    INC --> R2

    R2["R2 树维护（op 词表 §4.2.4）：<br/>· 派生新候选问题 spawn_question（写明 parent 与依赖声明）<br/>· 剪枝：agent propose_prune / 人类 prune_branch——写 decision(type=prune_branch) 后置 dead_end（非 I3 evidence）<br/>· 维护旧结论 applicability（needs_revalidation/contradicted→spawn revalidate 题）<br/>· 消费 inject_question / reprioritize / prune_branch / note"]:::phase
    R2 --> AMEND{"GOAL 已改版？"}:::dec
    AMEND -- "是（reasoning-only goal_amend 轮）：重评 open + 建 answer_applicability 适用性审计<br/>不批量重开 closed，受护栏（见 §13）" --> R3
    AMEND -- "否" --> R3

    R3["R3 选择出题（产 selection，不写 route）：对 open 问题集逐个显式四元打分（全程留痕）<br/>score(q) = w1·expected_gain + w2·info_gain − w3·cost_norm<br/>+ c·√( ln N / (1+visit_count) ) + directive_adjust"]:::phase
    R3 --> SEL{"与单轮预算 B(t) 比较后定 next_intent"}:::dec
    SEL -- "最优问题 est_cost ≤ B(t)" --> ATK["next_intent=attack：选定下一题<br/>下一轮进 idea；route 由编排器据 plan 派生（attack/eval_only/reuse_only/dependency_wait）"]:::phase
    SEL -- "最优问题 est_cost > B(t)" --> DCP["next_intent=decompose：下一轮 route=decompose（reasoning-only）<br/>下一轮才写子问题、挑子问题作再下轮攻坚"]:::phase
    SEL -- "三判据任一满足" --> TER(["next_intent=terminate：编排器收尾后停机<br/>（terminate 非 route，本轮 route 不改写）"]):::term

    ATK --> FIN
    DCP --> FIN
    TER --> FIN
    FIN["R4 收尾（原 closing 并入此处）：<br/>渲染 cycle_report.md → 增量渲染 views/（树/池/目标/摘要）<br/>→ Git 提交视图仓 → LEDGER 记账聚合 CYCLE.cost_total → 通知钩子"]:::phase
    FIN --> OUT["产物协议（符合 schema 的 JSON，编排器解析校验）<br/>answer.json {question_id, verdict, answer_md, evidence[]（带 metric_result_id）}（仅 answered/refuted；首轮/inconclusive 不产）<br/>tree_ops.json {ops[] ∈ 封闭七词表 §4.2.4} · selection.json {next_question_id, next_intent∈{attack,decompose,terminate}, scores[]}（不写 route，编排器派生）"]:::orch
    OUT -- "产物非法：重试后仍败" --> RF["轮次失败入 DECISION，等待下一轮或人工"]:::fail

    classDef phase fill:#dbeafe,stroke:#1d4ed8,color:#1e3a8a
    classDef asset fill:#dcfce7,stroke:#15803d,color:#14532d
    classDef human fill:#ffedd5,stroke:#c2410c,color:#7c2d12
    classDef gate fill:#ede9fe,stroke:#6d28d9,color:#4c1d95
    classDef fail fill:#fee2e2,stroke:#b91c1c,color:#7f1d1d
    classDef term fill:#e5e7eb,stroke:#4b5563,color:#1f2937
    classDef dec fill:#fef9c3,stroke:#a16207,color:#713f12
    classDef orch fill:#f1f5f9,stroke:#475569,color:#0f172a
```

---

## 图 03 · Idea：发散 · 统一审计 · 选定（§7）

**这张图回答**：选中的问题如何变成一个当前最优的可研究想法。关键质量门：**旁路想法也必须经过同一审计提示词**——两条路共用一个门；审计的独立性 = 独立 Runner 调用、只见产物不见生产过程。

<!-- png: 03-Idea -->
```mermaid
flowchart TD
    IN["读取：选中问题卡 Qn + 祖先链卡 · 该问题全部已试 IDEA 及结局（防重复造轮）<br/>相关已关闭问题的结论 + 相似失败卡 · 文献检索工具权限"]:::orch
    IN --> NEED{"需要全新创新吗？"}:::dec

    NEED -- "需要发散" --> GEN["① wildidea 跨域机制迁移（§7.5）<br/>源域冻结(原型编号·禁领域词)→映射研究领域→去锚点审查(成立等级)<br/>三层查重(词面/机制/退化)+最近邻差异+最强反驳+最小可证伪实验"]:::phase
    GEN --> SELF["② 自评筛选前 3（每条带源域机制/去锚点退化物/novelty_type）<br/>novelty 诚实：『联网粗查已启用·文献级待人工验证』，不写『查重通过』"]:::phase
    NEED -- "无需重大创新：旁路直接给出单一现成想法" --> AUDIT

    SELF --> AUDIT["③ 独立审计（wildidea 判官，统一质量门）<br/>独立上下文：只见映射(源域/目标域/对象映射/共享关系)、不见生产过程<br/>6 维 0-10：Structural Depth/Domain Distance/Applicability/Novelty/Unexpectedness/Non-Obviousness<br/>SD&lt;阈值(默认6)淘汰 · 评分写 DECISION（actor=judge）"]:::gate

    AUDIT --> PASS{"审计判定"}:::dec
    PASS -- "合格：选定 1 个" --> SELED["selected 标记唯一<br/>其余留 candidate / failed 记录在 IDEA 表（防复造）"]:::phase
    PASS -- "全部候选不合格" --> FAILI["阶段失败（研究轮正常收尾，cycle=done，非系统失败）写明理由入账<br/>→ 汇入本轮 reasoning 收尾；问题置 inconclusive、visit_count+1<br/>（下一轮 reasoning 自行裁决：换 idea / 降级 / 分解）"]:::fail

    SELED --> OUT["产物 idea_set.json<br/>{candidates[], audit_scores[], selected_id, novelty_refs[]}<br/>门禁写入：全部候选与评分写 IDEA + DECISION"]:::orch
    OUT --> NXT["进入 Plan"]:::phase

    classDef phase fill:#dbeafe,stroke:#1d4ed8,color:#1e3a8a
    classDef asset fill:#dcfce7,stroke:#15803d,color:#14532d
    classDef human fill:#ffedd5,stroke:#c2410c,color:#7c2d12
    classDef gate fill:#ede9fe,stroke:#6d28d9,color:#4c1d95
    classDef fail fill:#fee2e2,stroke:#b91c1c,color:#7f1d1d
    classDef term fill:#e5e7eb,stroke:#4b5563,color:#1f2937
    classDef dec fill:#fef9c3,stroke:#a16207,color:#713f12
    classDef orch fill:#f1f5f9,stroke:#475569,color:#0f172a
```

---

## 图 04 · Plan：复用四情形 · 锁评估协议（§4 §7.2）

**这张图回答**：selected idea 如何与 baseline 池发生关系，产出可执行、可回答的计划。两个关键锁：**复用规则由 plan 机械执行**（四情形）；**评估协议在此锁死，bundle 只照办不再发明**（I2 源头）。占坑互斥（I5）防止昂贵构建重复开工。`targets[] target_kind ∈ {build, exec, eval}`；import 走 `import_defer` 单独分支（deferred、不产 target，§3.6.3）。

<!-- png: 04-Plan -->
```mermaid
flowchart TD
    IN["读取：selected idea · baseline 池（规格索引：canonical_key 唯一 + baseline_tag）<br/>测量索引 (variant_id, protocol_id, protocol_ver) 复用判定 O(1) · 历史协议与指标库 · 单轮预算 B(t)"]:::orch
    IN --> NEEDS["① 把 idea 要验证的核心点拆成 verification needs"]:::phase
    NEEDS --> REUSE{"② 逐 need 复用判定（查 metric_result 覆盖，机械执行）"}:::dec

    REUSE -- "同协议@版本 + 所需指标已测 + env 兼容" --> RA["直接引用历史 evaluation 作证据<br/>成本=零 · **不产 target**（I1 保证可比）"]:::asset
    REUSE -- "协议场景升版 / 所需指标尚未测" --> RB["target_kind=eval：拿既有 checkpoint 免训练重评<br/>场景升版→新 evaluation；加指标→追加 attempt（成本低）"]:::asset
    REUSE -- "池中有：env 失配 / 结果存疑" --> RC["target_kind = eval：复现性评估<br/>执行 identity 的一键复现命令（成本低）"]:::asset
    REUSE -- "池中无 + 自建角色（消融/替换/超参/评估）" --> RD["③ 立项：target_kind = build（新 baseline）/ exec（同 baseline 新变体训练）<br/>/ eval（免训评估，按 §3.3.1 判定）—— build/exec 成本高，标依赖 DAG"]:::phase
    REUSE -- "池中无 + 引入外部 baseline 家族/参照槽" --> IMP["③' import 三闸（§3.3.2）→ 准入链（§3.6.3）<br/>M1–M3 deferred（import_defer，不入 targets[]）：external_candidate 经 import_register 登记；<br/>phase_commit 业务三写入（与 set_route(dependency_wait)+释放 Qn 回 open+mark_cycle_done 同一事务，§4.2.5）：<br/>external_import(selected_for_materialization) + 占位 baseline(planned) + question_dep(pending) —— 不产可执行 target<br/>M4：Advancer 独立物化 worker cycle 供给，dep satisfied 后问题回可调度集"]:::phase

    RA --> PROTO
    RB --> PROTO
    RC --> PROTO
    RD --> PROTO
    IMP --> WAIT

    PROTO["④ 定义本轮评估 PROTOCOL（场景不可变，改场景升 version I1）<br/>+ METRIC_DEF 集（可追加 I2）+ 每指标判读规则 readout_rule + smoke 定义 + 成本预算"]:::phase

    PROTO --> REVIEW["可回答性清单：独立评审会话（≤2 轮）逐条核验<br/>· 每个 need ≥1 个指标 · 每个指标有判读规则<br/>· 每个构建/训练计划有 smoke 定义与预算 · 指标全部由协议声明（I2 源头）<br/>★ 评估协议在此锁死——bundle 阶段只照办、不再发明"]:::gate

    REVIEW -- "通过" --> GATES["门禁与写入：<br/>gate_new_protocol 写协议与指标（I1：场景升版 / 指标可追加）<br/>build → gate_claim_baseline 占坑 planned（I5 互斥·UNIQUE canonical_key）+ claim variant<br/>exec → gate_claim_variant（同 baseline 新变体占坑）<br/>按依赖序写 build_target 队列（seq + critical + target_kind）"]:::gate
    REVIEW -- "两轮仍不过" --> FAILP["阶段失败（研究轮正常收尾，cycle=done，非系统失败）入账<br/>→ 汇入 reasoning 收尾；问题置 inconclusive、visit+1，下一轮决定换 idea 或分解"]:::fail

    GATES --> DEP{"所需 baseline 正被他轮 building（互斥占用）？"}:::dec
    DEP -- "是：写 QUESTION_DEP(dep_type=baseline) 等待，不重复开工（I5）" --> WAIT["本轮 dependency_wait 收尾：写 dep + 释放 Qn 回 open + mark_cycle_done（§4.2.5，不经 reasoning）"]:::asset
    DEP -- "否" --> OUT["产物 plan.json {needs[], reuse_evidence[],<br/>targets[]（target_kind ∈ {build,exec,eval}, 依赖序, 预算；eval 带 eval_action/attempt_purpose/evaluation_id?）,<br/>import_defer?（deferred 决定，不产 target）, protocol, metric_defs[], readout_rules[]}"]:::orch

    OUT --> DEG{"targets 是否为空？"}:::dec
    DEG -- "空 → route=reuse_only：跳过 bundle，reasoning 直接引用历史 evaluation" --> NXR(["纯复用轮：不进 bundle，直接轮尾 reasoning"]):::term
    DEG -- "仅 eval → route=eval_only：仍进 bundle（只评估、不训练）" --> NXT
    DEG -- "含 build/exec → 串行构建" --> NXT["进入 Bundle"]:::phase

    classDef phase fill:#dbeafe,stroke:#1d4ed8,color:#1e3a8a
    classDef asset fill:#dcfce7,stroke:#15803d,color:#14532d
    classDef human fill:#ffedd5,stroke:#c2410c,color:#7c2d12
    classDef gate fill:#ede9fe,stroke:#6d28d9,color:#4c1d95
    classDef fail fill:#fee2e2,stroke:#b91c1c,color:#7f1d1d
    classDef term fill:#e5e7eb,stroke:#4b5563,color:#1f2937
    classDef dec fill:#fef9c3,stroke:#a16207,color:#713f12
    classDef orch fill:#f1f5f9,stroke:#475569,color:#0f172a
```

---

## 图 05 · Bundle：串行构建 · 训练 · 评估 · 注册（§7 §9）

**这张图回答**：缺失的 baseline/变体如何被合法地造出来并测量。主干 = 单个目标的生命周期（**严格串行，同一时刻只有一个在建目标**）。一次执行可产**多个 checkpoint**（如 LOSO 15 折）；评估产出 **evaluation + metric_result(fold + aggregate)**；**复制非剪切**入池。「构建完成且有结果后才入库」由 I4 机械保证。

<!-- png: 05-Bundle -->
```mermaid
flowchart TD
    LOAD["按依赖序载入 build_target 队列（target_kind ∈ {build, exec, eval}）<br/>输入：plan.json · identity 模板 · 评估协议与指标定义 · 环境锁定文件 · 资源限额"]:::orch
    LOAD --> PICK{"队列中还有 pending 目标？"}:::dec
    PICK -- "否：队列空 → 轮尾 reasoning" --> CLS
    PICK -- "是：取下一个（严格串行）" --> KIND{"target_kind？"}:::dec

    KIND -- "eval（免训练再评估）" --> EVAL_E["免训练评估：用既有 legal 变体的 checkpoint(s) 评估<br/>→ 评估结果落 staging（fold + aggregate）"]:::phase
    KIND -- "build / exec（需构建或训练）" --> WT
    EVAL_E -- "评估失败 / 超时" --> RFAIL
    EVAL_E --> RVE["结果评审（result_review，≤3 轮 · policy）<br/>staging 结果 + 运行日志合理性 · 据结果反查配置/流程问题<br/>（独立 judge 会话，DECISION 带 subject_hash，§4.1.4 附注）"]:::gate
    RVE -- "轮数用尽 → review_failed" --> RFAIL
    RVE -- "通过" --> V3E["三级校验（I2 履约）"]:::gate
    V3E -- "失败：重试仍败" --> RFAIL
    V3E -- "通过" --> REGE["gate_register_evaluation：按 eval_action 定位或创建 evaluation<br/>已存在(同 variant,protocol@版本)→追加 attempt；不存在→建 evaluation+首 attempt<br/>（注册段单事务；不重注册 baseline/variant、不绑出厂测量）"]:::gate
    REGE --> PICK

    WT["开 Git worktree（cycle&lt;id&gt;-&lt;key&gt;）（§7 护栏）<br/>写权限仅限 worktree + artifacts · 网络默认关闭（仅 allowlist）<br/>环境以 lock 文件锁定 · Limits 由 watchdog 强制"]:::orch
    WT --> BUILD["Codex 构建主体代码 / 配置变体（config + overrides，不复制整份 src）<br/>（一任务一会话，无记忆，无库凭据）"]:::phase
    BUILD --> SMOKE{"smoke 测试（修复循环无次数上限，受预算 + watchdog）"}:::dec
    SMOKE -- "通过" --> RVA["代码适配评审（code_review，≤3 轮 · policy）<br/>独立子智能体只见 plan + 代码产物 + smoke 结论<br/>核对实现与 plan 适配 · 无缩水 · 无不当实现（§3.4.3）"]:::gate
    SMOKE -- "预算尽 + 自动追加一次 + 换会话仍败" --> ENGBLK["目标置 engineering_blocked + 通知人<br/>（工程失败永不入问题树）"]:::fail
    ENGBLK --> CLS

    RVA -- "不过：意见回传构建会话修复" --> BUILD
    RVA -- "轮数用尽 → review_failed（不训练，省成本）" --> RFAIL
    RVA -- "通过（pass DECISION + subject_hash 相符 → smoke→running）" --> FULL

    FULL["训练执行 run(kind = build|exec) → 产出 checkpoint(s)<br/>（段(i)执行事实随发生入账：开训即 running · finish_run + checkpoint + run-owned log）<br/>写 identity.md 草稿（名称/用途/复现命令/环境指纹）"]:::phase
    FULL -- "训练失败 / watchdog 超时 → run(failed)" --> RFAIL
    FULL --> EVAL["出厂评估(source=factory)：用锁定评估协议@版本评估<br/>→ 评估结果落 staging（fold + aggregate）"]:::phase
    EVAL -- "评估失败/超时：run 仍 success、attempt(failed)、baseline 不 legal" --> EFAIL["写 evaluation_attempt(failed) + 失败卡<br/>（训练已成、评估未过 → 目标 failed）"]:::fail
    EFAIL --> CRIT
    EVAL --> RVB["结果评审（result_review，≤3 轮 · policy）<br/>对象一律 staging 产物：结果 artifact + checkpoint hash + 日志/观测 staging 文件<br/>结果与 log 合理性 · 据结果反查代码有无明显 bug（log 仅供 judge 读，不进门禁/证据）"]:::gate
    RVB -- "不过：意见回传修复重评" --> EVAL
    RVB -- "轮数用尽 → review_failed（测量整包不注册；run+checkpoint 保留）" --> RFAIL
    RVB -- "通过" --> V3["编排器三级校验入库（确定性仲裁 §5）<br/>① JSON schema → ② 引用完整性 → ③ 业务门禁<br/>I2 在此机械执行：metric_result 出现协议未声明的指标即当场拒绝"]:::gate
    V3 -- "通过：段(ii)注册单事务写 evaluation + attempt(success) + metric_result + attempt-owned log 补登" --> REG
    V3 -- "任一级失败：记 DECISION(actor=gate, reject) 重试，仍败" --> RFAIL

    REG["build → gate_register_baseline（I4）：identity 完备 + smoke 通过 + 协议声明齐全 + 复现命令可执行 → baseline+variant 置 legal · 绑出厂 evaluation<br/>exec → gate_register_variant：variant 置 legal · 绑 checkpoint/evaluation（不重注册 baseline）<br/>→ 复制（非剪切）入 baselines/ 主干并打 tag · 刷新基线/变体卡 · 回收 worktree · 目标置 complete"]:::gate
    REG --> PICK

    RFAIL["训练/构建/评审失败：写 run(failed, failure_kind ∈ {build, smoke, timeout, runtime, data_invalid, aborted}) + 失败卡<br/>（评审否决 = target failed(review_failed)：run 若已 success 则保留，测量不注册；eval-only 的评估失败写 evaluation_attempt(failed)）<br/>（P4 失败即观测：不兜底、不改计划，裁决权属轮尾 reasoning）"]:::fail
    RFAIL --> CRIT{"该目标 critical？"}:::dec
    CRIT -- "是：早退——跳过剩余目标，带失败摘要" --> CLS
    CRIT -- "否：目标置 skipped，取下一个" --> PICK

    CLS["队列空 / 早退 → 汇入轮尾 Reasoning（答题 + 收尾）"]:::phase

    WDOG["watchdog：每次 Runner 调用带 wall-clock 上限<br/>transcript mtime 基础心跳（综合判活 二部§6.7）→ 超时即终止会话写 failed"]:::orch
    WDOG -.-> BUILD
    WDOG -.-> FULL
    WDOG -.-> EVAL
    WDOG -.-> EVAL_E

    classDef phase fill:#dbeafe,stroke:#1d4ed8,color:#1e3a8a
    classDef asset fill:#dcfce7,stroke:#15803d,color:#14532d
    classDef human fill:#ffedd5,stroke:#c2410c,color:#7c2d12
    classDef gate fill:#ede9fe,stroke:#6d28d9,color:#4c1d95
    classDef fail fill:#fee2e2,stroke:#b91c1c,color:#7f1d1d
    classDef term fill:#e5e7eb,stroke:#4b5563,color:#1f2937
    classDef dec fill:#fef9c3,stroke:#a16207,color:#713f12
    classDef orch fill:#f1f5f9,stroke:#475569,color:#0f172a
```

---

## 图 06 · 核心实体状态机 · 对象关系 · 机制图（§4 §9 §10 §12）

**这张图回答**：库里哪些状态迁移合法、对象之间什么关系、资产如何被召回与流转。**状态机由门禁强制：非法迁移在写库时报错**。不变量执行点：I1=gate_new_protocol、I2=metric_result 触发器、I3=gate_close_question、I4=gate_register_baseline、I5=gate_claim_baseline/variant、I6=selector 规范化+gate 校验（写 target_set_hash）、append-only=各写库点冻结约束/终态冻结触发器。

### 06a · QUESTION（问题树节点）——派生向下，关闭向上

<!-- png: 06a-QUESTION状态机 -->
```mermaid
stateDiagram-v2
    direction LR
    [*] --> open: 创建（agent 派生 / human 注入 inject_question）
    open --> active: reasoning 路由选中（攻坚）
    active --> open: decompose 释放（写子问题+逐子 dep）/<br/>dependency_wait 释放（plan 新写 dep）/<br/>cycle failed·aborted 释放（§4.2.3）——等待与系统失败非尝试、不增 visit
    active --> answered: gate_close_question（I3）必须 ≥1 条 EVIDENCE
    active --> refuted: 同上（证据指向否定）
    active --> inconclusive: mark_inconclusive（证据不足 / 本轮失败，visit_count+1）
    inconclusive --> active: 调度重选（可调度 ⟺ 无 pending dep；attack 另受 max_inconclusive_per_question，§4.2.1）
    open --> dead_end: 剪枝——agent/human 写 decision(type=prune_branch) 后置 dead_end（非 I3 evidence）
    answered --> [*]
    refuted --> [*]
    dead_end --> [*]
    note right of answered
        父节点由子答案聚合关闭（evidence = child_answer）
        证据指向 evaluation 指标；根问题最后关闭是设计使然
        回看不重开：closed 节点下可新增 source=revalidate 子题（applicability 见 §4.6.4）
    end note
```

### 06b · BASELINE（结构资产）——building 即互斥锁

<!-- png: 06b-BASELINE状态机 -->
```mermaid
stateDiagram-v2
    direction LR
    [*] --> planned: gate_claim_baseline 占坑（I5 canonical_key 全池唯一·UNIQUE）
    planned --> building: bundle 开工（building 即互斥：他轮同键只能声明依赖等待）
    building --> legal: gate_register_baseline（I4）<br/>identity 完备 + smoke 通过 + 协议声明齐全 + 复现命令存在<br/>且绑定出厂 evaluation（出厂测量）
    building --> build_failed: 构建 / smoke / 训练 / 评估失败
    build_failed --> planned: 重试
    build_failed --> abandoned: 放弃
    legal --> deprecated: 弃用（被替代，证据保留）
    abandoned --> [*]
    deprecated --> [*]
    note right of legal
        「构建完成且有结果后才入库」的形式化：
        注册前提 = 训练成功 + 出厂 evaluation 指标全部产出
    end note
```

### 06c · VARIANT（结构 + 训练设置，一次执行）——append-only

<!-- png: 06c-VARIANT状态机 -->
```mermaid
stateDiagram-v2
    direction LR
    [*] --> planned: plan claim 变体（UNIQUE(baseline_id, variant_key)）
    planned --> building: bundle 训练执行 run(kind=build|exec)
    building --> legal: 产出 checkpoint(s) + 出厂 evaluation 入库注册
    building --> build_failed: 训练 / 评估失败
    build_failed --> abandoned: 放弃
    legal --> deprecated: 弃用（证据保留）
    abandoned --> [*]
    deprecated --> [*]
    note right of legal
        append-only：config + overrides + checkpoint + evaluation
        一经构建即冻结；改训练设置 = 新变体；升级评估协议 = 追加 evaluation
        仅 status 可迁移
    end note
```

### 06d · RUN（训练执行中心事实表）——成败同记，kind ∈ {build, exec}

<!-- png: 06d-RUN状态机 -->
```mermaid
stateDiagram-v2
    direction LR
    [*] --> created: 创建（kind ∈ {build=构建, exec=训练执行}；免训练评估不建 run）
    created --> running: Runner 启动（带 wall-clock 上限 + 心跳）
    running --> success: 产出 checkpoint(s)；记 commit_hash + env_hash + cost
    running --> failed: 必带 failure_kind ∈ {build, smoke, timeout, runtime, data_invalid, aborted}
    success --> [*]
    failed --> [*]: P4 失败与成功同等级别入库 + 生成失败卡
```

### 06e · EVALUATION（对照元：逻辑测量单元 = 变体 × 评估协议@版本）

<!-- png: 06e-EVALUATION状态机 -->
```mermaid
stateDiagram-v2
    direction LR
    [*] --> created: plan 安排评估（evaluation.source ∈ {factory, protocol_upgrade, standalone_eval}）
    created --> running: 首个 attempt 启动
    running --> success: 某 attempt 成功 → 标 canonical_attempt
    running --> failed: 重试耗尽（gate_finish_evaluation→failed，防永久 running）
    failed --> running: 允许后续再试（追加 attempt）
    success --> success: 追加非 canonical 复现 attempt（不替换基准）
    success --> [*]: 可被 EVIDENCE 引用；测量索引 O(1) 复用
    success --> abandoned: 仅当未被 valid evidence 引用（gate_abandon_evaluation）；证据 append-only 不可撤，已被引用即永不可弃用
    failed --> abandoned: 放弃该评估格子（不再重试）
    abandoned --> [*]
    note right of success
        evaluation 是逻辑单元；真实执行在 evaluation_attempt（见 06e2）
        升级评估协议 = 追加新 evaluation（append-only）
    end note
```

### 06e2 · EVALUATION_ATTEMPT（一次真实评估执行）——审计载体

<!-- png: 06e2-EVALUATION_ATTEMPT状态机 -->
```mermaid
stateDiagram-v2
    direction LR
    [*] --> created: 在 evaluation 下开一次执行（attempt_no, purpose）
    created --> running: Runner 启动（带 wall-clock + 心跳）
    running --> success: 写 metric_result（fold + aggregate，I2）<br/>记 env_hash / commit_hash / transcript / cost
    running --> failed: 必带 failure_kind ∈ {timeout, runtime, data_invalid, metric_missing, env_invalid, ...}
    running --> aborted: 人类 abort / 全局停机
    success --> [*]: 可被 canonical_attempt 选中
    failed --> [*]: 失败 attempt 保留（append-only）；retry_of 追加新 attempt
    aborted --> [*]
```

### 06f · BUILD_TARGET（bundle 串行队列）——每个状态即断点

<!-- png: 06f-BUILD_TARGET状态机 -->
```mermaid
stateDiagram-v2
    direction LR
    [*] --> pending: plan 按依赖序入队（seq + critical + target_kind）
    pending --> building: gate_start_build_target（build：baseline+variant→building / exec：仅 variant→building，baseline 保持 legal）· 开 worktree（严格串行）
    pending --> running: eval：免训练，直接评估（无 building/smoke）
    building --> smoke: 构建主体完成
    smoke --> running: smoke 通过且代码适配评审通过（gate_progress：pass DECISION + subject_hash 相符，§4.1.4）
    smoke --> failed: 代码适配评审轮数用尽（review_failed，不训练）
    running --> complete: 结果评审通过 → build→…+gate_register_baseline；exec→…+gate_register_variant；eval→…+gate_register_evaluation（注册段单事务）
    smoke --> engineering_blocked: 预算尽 + 自动追加一次 + 换会话仍败（通知人，工程失败不入问题树）
    running --> failed: 训练/评估失败 / 超时 / 校验拒绝 / 结果评审轮数用尽（review_failed，测量不注册）
    failed --> [*]: critical → 轮次早退；剩余 pending 不再执行
    pending --> skipped: 非关键目标失败旁路 / 早退跳过
    complete --> [*]
    skipped --> [*]
    engineering_blocked --> [*]
```

### 06g · CYCLE（一轮循环）——reasoning 是裁决与作答的唯一收口（dependency_wait 为 plan 机械收尾）

<!-- png: 06g-CYCLE状态机 -->
```mermaid
stateDiagram-v2
    direction LR
    [*] --> created: advance 创建轮次（先消费 DIRECTIVE 收件箱）
    created --> reasoning: bootstrap首轮 / decompose / goal_amend（reasoning-only，无前置三阶段）
    created --> idea: 非首轮攻坚（本轮已有攻坚目标 Qn）
    idea --> plan: 审计选定 selected 唯一
    idea --> reasoning: 全部候选不合格（带失败汇入收尾）
    plan --> bundle: targets 含 build/exec/eval（route=attack/eval_only）
    plan --> reasoning: 纯复用 targets=[]（route=reuse_only）/ 评审两轮不过
    plan --> done: dependency_wait（写 dep + 释放 Qn 回 open + mark_cycle_done，§4.2.5，不经 reasoning）
    bundle --> reasoning: 队列完成 / 关键目标失败早退
    reasoning --> done: 答题 + 选择(selection) + 收尾完成（cycle.route ∈ {bootstrap,attack,decompose,reuse_only,eval_only,goal_amend}；dependency_wait 经 plan --> done）
    reasoning --> failed: reasoning 产物非法 / 重试仍败（带失败摘要收尾，Qn 回 open）
    reasoning --> aborted: 仅人类 abort_cycle / 异常中止（非正常终止）
    done --> [*]: selection.next_intent=terminate → 全局停机；否则下次唤醒开下一轮
    failed --> [*]
    aborted --> [*]
    note right of created
        每个状态即恢复检查点（P6）：任意时刻 kill -9，重启读状态字段即续跑。
        无独立 closing：裁决/作答轮收尾并入 reasoning 轮尾（dependency_wait 为 plan 机械收尾，§4.2.5）；轮次失败 = reasoning 带失败摘要收尾
    end note
```

### 06h · 对象关系（三正交维度：结构 × 训练设置 × 评估协议）

**这张图回答**：对象模型的连接结构。**对照元 = evaluation**；**variant 与 protocol 之间没有边**（训练设置在 variant.config，评估协议只经 evaluation 进入）。

<!-- png: 06h-对象关系 -->
```mermaid
flowchart TD
    BL["baseline<br/>结构 / 前向逻辑<br/>canonical_key UK · parent_id 自引用方法族树"]:::asset
    VR["variant<br/>结构 + 训练任务设置<br/>config_json（训练设置）· 不绑评估协议"]:::asset
    CK["checkpoint<br/>被评估对象（一变体可多个，如 LOSO 15 折）"]:::asset
    RN["run<br/>训练执行 kind ∈ {build, exec}"]:::orch
    EV["evaluation ★ 对照元 ★<br/>变体 × 评估协议@版本（逻辑测量单元）"]:::gate
    AT["evaluation_attempt<br/>一次真实执行（env/transcript/重试）"]:::orch
    MR["metric_result<br/>scope ∈ {fold, aggregate}"]:::asset
    PR["protocol<br/>评估协议（场景不可变 I1，按 version）"]:::asset
    MD["metric_def<br/>指标定义（direction/unit/judgment）"]:::asset
    PM["protocol_metric<br/>协议声明的指标集（可追加 I2）"]:::asset

    BL -->|"1 ─ *"| VR
    VR -->|"1 ─ *"| CK
    VR -->|"1 ─ * 训练"| RN
    RN -->|"1 ─ * 产出"| CK
    VR -->|"1 ─ * 评估"| EV
    EV -->|"1 ─ * 执行"| AT
    AT -->|"1 ─ *"| MR
    EV -.->|"canonical_attempt"| AT
    MR -.->|"*─1 哪一折"| CK
    MR -.->|"*─1"| MD
    EV -->|"*─1 引用@版本"| PR
    PR -->|"经 protocol_metric *─*"| PM
    PM -.-> MD

    NOTE1["对照 = 同评估协议下 ≥2 个 evaluation<br/>比同一指标（可跨 baseline）"]:::term
    EV -.-> NOTE1
    NOTE2["✗ variant 与 protocol 无直接边<br/>评估协议只经 evaluation 进入对象模型"]:::fail
    VR -.-> NOTE2

    classDef phase fill:#dbeafe,stroke:#1d4ed8,color:#1e3a8a
    classDef asset fill:#dcfce7,stroke:#15803d,color:#14532d
    classDef human fill:#ffedd5,stroke:#c2410c,color:#7c2d12
    classDef gate fill:#ede9fe,stroke:#6d28d9,color:#4c1d95
    classDef fail fill:#fee2e2,stroke:#b91c1c,color:#7f1d1d
    classDef term fill:#e5e7eb,stroke:#4b5563,color:#1f2937
    classDef dec fill:#fef9c3,stroke:#a16207,color:#713f12
    classDef orch fill:#f1f5f9,stroke:#475569,color:#0f172a
```

### 06i · 资产召回（渐进四级，plan 复用判定最重）

<!-- png: 06i-资产召回 -->
```mermaid
flowchart LR
    Q["plan：need 复用判定"]:::phase --> L1
    L1["① 初筛：语义检索 + faceted tag<br/>命中 方法族卡 / 基线卡"]:::asset
    L1 --> L2["② 中筛：读基线卡变体矩阵<br/>定位候选变体"]:::asset
    L2 --> L3["③ 精确：评估协议@版本 + 指标匹配<br/>测量索引 (variant_id, protocol_id, protocol_ver) O(1) 命中成功 evaluation"]:::gate
    L3 --> L4["④ 深潜：ctx-fetch 按 ref 进入<br/>evaluations/ 或 src/ 读原始内容"]:::orch
    L3 -. "命中 → 引用历史 evaluation，零新评估" .-> HIT(["复用成立：成本归零"]):::term
    L3 -. "未命中 → 升版评估 / 新变体 / 新 baseline" .-> MISS["回 plan 安排 target_kind"]:::phase
    classDef phase fill:#dbeafe,stroke:#1d4ed8,color:#1e3a8a
    classDef asset fill:#dcfce7,stroke:#15803d,color:#14532d
    classDef gate fill:#ede9fe,stroke:#6d28d9,color:#4c1d95
    classDef term fill:#e5e7eb,stroke:#4b5563,color:#1f2937
    classDef orch fill:#f1f5f9,stroke:#475569,color:#0f172a
```

### 06j · md 文档三类生命周期

<!-- png: 06j-md生命周期 -->
```mermaid
flowchart TD
    subgraph T1["① 真相载体（identity / summary / protocol spec / cycle_report / handoff / 阶段产物）"]
        A1["草稿（staging）"]:::phase --> A2["入库（门禁写 DB + 登记路径）"]:::gate --> A3["转正（并入主干，转权威）"]:::asset --> A4(["冻结 / 归档"]):::term
    end
    subgraph T2["② DB 投影（views/ 每轮渲染 · 卡片物化 DB card 表不落文件）"]
        B1["从 DB 渲染"]:::orch --> B2["可删可重建（丢了重渲染）"]:::asset
    end
    subgraph T3["③ 临时快照（context_pack / transcript）"]
        C1["每个 turn 产生"]:::orch --> C2(["归档供回放，非长期真相"]):::term
    end
    DB[("SQLite 唯一真相")]:::asset
    DB -. "渲染" .-> B1
    A3 -. "真相回写" .-> DB
    classDef phase fill:#dbeafe,stroke:#1d4ed8,color:#1e3a8a
    classDef asset fill:#dcfce7,stroke:#15803d,color:#14532d
    classDef gate fill:#ede9fe,stroke:#6d28d9,color:#4c1d95
    classDef term fill:#e5e7eb,stroke:#4b5563,color:#1f2937
    classDef orch fill:#f1f5f9,stroke:#475569,color:#0f172a
```

### 06k · baseline 池管理两层面（文件层 ∥ DB 层，经入库三段缝合）

<!-- png: 06k-池管理两层面 -->
```mermaid
flowchart LR
    subgraph FILE["文件层（磁盘）"]
        direction TB
        F1["构建现场<br/>questions/&lt;q&gt;/cycles/&lt;id&gt;/build/&lt;key&gt;/"]:::phase
        F2["复制（非剪切）入池<br/>baselines/&lt;b&gt;/{identity.md, src/, variants/}"]:::asset
        F3["大文件只引用不复制<br/>变体不复制代码（config + overrides）"]:::asset
        F1 --> F2 --> F3
    end
    subgraph DBL["DB 层（research.sqlite）"]
        direction TB
        D1["状态机 + 门禁不变量"]:::gate
        D2["索引：规格 canonical_key+tag+parent_id<br/>测量 (variant,protocol,ver) O(1) · 语义/词法卡片"]:::asset
        D1 --> D2
    end
    SEW["入库三段缝合：草稿 → 入库（门禁写 DB + 登记路径）→ 转正（文件并主干 + identity 转权威 + 刷 pool.md/卡片）"]:::gate
    FILE -. "草稿" .-> SEW
    SEW -. "登记" .-> DBL
    classDef phase fill:#dbeafe,stroke:#1d4ed8,color:#1e3a8a
    classDef asset fill:#dcfce7,stroke:#15803d,color:#14532d
    classDef gate fill:#ede9fe,stroke:#6d28d9,color:#4c1d95
    classDef orch fill:#f1f5f9,stroke:#475569,color:#0f172a
```

### 06l · 一轮 IO 数据流（四阶段 context_pack → 产物 → 门禁入库）

<!-- png: 06l-一轮IO数据流 -->
```mermaid
flowchart LR
    DB[("SQLite 真相")]:::asset
    DB ==> CP["上下文编译器<br/>四区包"]:::orch
    CP --> Ii["idea：问题卡+祖先+已试 idea+失败卡+文献"]:::phase
    Ii -->|"idea_set.json"| Gi["门禁→IDEA+DECISION"]:::gate
    Gi --> Pp["plan：idea+池卡+协议库+预算"]:::phase
    Pp -->|"plan.json"| Gp["门禁→protocol+占坑+build_target"]:::gate
    Gp --> Bb["bundle：计划切片+评估协议+identity 模板+env"]:::phase
    Bb -->|"variant+checkpoint+evaluation+attempt+metric_result"| Gb["门禁→复制入池"]:::gate
    Gb --> Rr["reasoning：本轮问题卡+本轮 bundle 结果+子树摘要+召回"]:::phase
    Rr -->|"answer/tree_ops/selection"| Gr["门禁→关问题+写树+收尾"]:::gate
    Gr ==> DB
    classDef phase fill:#dbeafe,stroke:#1d4ed8,color:#1e3a8a
    classDef asset fill:#dcfce7,stroke:#15803d,color:#14532d
    classDef gate fill:#ede9fe,stroke:#6d28d9,color:#4c1d95
    classDef orch fill:#f1f5f9,stroke:#475569,color:#0f172a
```

### 06m · baseline 池生命周期 ①–⑧

<!-- png: 06m-池生命周期 -->
```mermaid
flowchart TD
    S1["① 立项：结构变 → 新 baseline<br/>gate_claim_baseline(I5) planned · claim variant · build_target(build)"]:::phase
    S2["② 构建+训练+出厂评估<br/>run(build)→checkpoint(s) · evaluation+attempt+metric_result(I2) · gate_register(I4)→legal · 复制入池"]:::phase
    S3["③ 引用：某 evaluation 指标作证据<br/>EVIDENCE(question→evaluation)"]:::asset
    S4["④ 加变体：改训练设置 → 新变体(exec)<br/>run(exec)→新 checkpoint(s) · evaluation · variants/&lt;新key&gt;/"]:::phase
    S5["⑤ 升级评估协议：不动训练，拿 checkpoint 重评<br/>场景升版→新 evaluation；加指标→同 evaluation 追加 attempt"]:::phase
    S6["⑥ 复用：测量索引命中成功 evaluation → O(1) 引用，零新评估"]:::asset
    S7["⑦ 派生 baseline：结构变 → 新 baseline(parent 指原)，走 ②"]:::phase
    S8["⑧ 弃用：legal → deprecated（只标记，证据保留）"]:::term

    S1 --> S2 --> S3
    S2 --> S4
    S2 --> S5
    S4 --> S6
    S5 --> S6
    S2 --> S7
    S2 --> S8
    INV["贯穿不变量：I5 占坑① · I4 注册② · I2 测量履约②⑤ · I1 协议场景不可变⑤ · append-only ⑤⑧"]:::gate
    classDef phase fill:#dbeafe,stroke:#1d4ed8,color:#1e3a8a
    classDef asset fill:#dcfce7,stroke:#15803d,color:#14532d
    classDef gate fill:#ede9fe,stroke:#6d28d9,color:#4c1d95
    classDef term fill:#e5e7eb,stroke:#4b5563,color:#1f2937
    classDef orch fill:#f1f5f9,stroke:#475569,color:#0f172a
```

---

## 图 07 · 人类审计与介入：web + QQ + 自然语言（§13）

**这张图回答**：人如何在不破坏不变量的前提下看懂、改变系统。两原则：**人不直接改库（P5）**——经 DIRECTIVE 收件箱由门禁在指定时机消费；**人与系统记同一本账**——指令消费与系统决策同写 DECISION。三通道：web 只读仪表盘 / QQ bot 推送与收 NL / 自然语言转 directive。

<!-- png: 07-人类审计与介入 -->
```mermaid
flowchart LR
    H["人类<br/>（不直接改库 P5）"]:::human

    subgraph CHAN["三通道（借鉴 DeepScientist 异步消息队列 / channels connector / artifact.interact）"]
        direction TB
        C1["web 只读仪表盘<br/>tree.md / pool.md / goal.md / digest.md"]:::asset
        C2["QQ bot：推送（轮失败 / engineering_blocked / 每 K 轮摘要 / 文件请求三事件）<br/>+ 接收自然语言消息"]:::human
        C3["CLI / 落 directives/inbox/ JSON 文件"]:::human
    end
    H --> CHAN
    C2 --> NL["长在线中介线程（只读 Codex）：解答 query · 双向传话 ·<br/>润色用户请求 → 硬指令回显确认展示润色稿 · 软指令直接入队"]:::gate
    C3 --> DT
    NL -- "raw 原样入 interaction_message（不可变）；<br/>润色稿进 directive.payload_json" --> DT["DIRECTIVE 表 status = pending"]:::asset

    DT --> T1["立即生效（硬）：set_budget"]:::gate
    DT --> T2["阶段边界（硬）：pause / resume / abort_cycle（问题回 open）"]:::gate
    DT --> T3["下一轮 reasoning 轮始消费<br/>inject_question(软) · reprioritize(pin 硬/boost·suppress 软)<br/>prune_branch(硬，置 dead_end + decision(type=prune_branch)；非 I3 evidence) · goal_amend(硬，目标新版本) · note(软)"]:::gate

    T1 --> DEC["消费写 DECISION（人与系统同一本账）<br/>软指令 = 采集打分加成项：系统可有理由地不从，须写明理由<br/>硬指令 = 绕过权衡直接生效"]:::asset
    T2 --> DEC
    T3 --> DEC

    RQ["文件请求（§4.6.8）：阶段 sidecar resource_request.json（强制披露已尝试路径+失败原因）<br/>→ interaction_request(pending)（一 goal 一张 pending 单）→ 通知 request_pending/reminder<br/>→ 全局等待：只停研究推进，query/通知/上传校验照常"]:::gate
    RQ -.-> C2
    H -- "放文件入 uploads/&lt;request_id&gt;/&lt;item_no&gt;/ · 控制台答复 已提供/无法提供/取消" --> RQD["daemon 校验 hash 入账 → 并入 input/user_provided/&lt;request_id&gt;/（流程层只读）<br/>resolution_json + resolved_message_id → resolved/cancelled → Advancer 恢复<br/>（文件是输入资产、非 evidence；请求/答复不写 decision）"]:::asset

    T3 -- "改变研究方向" --> GA["goal_amend → GOAL 新版本（旧版不可变 G1 锚）<br/>→ 下一轮强制重评全部 open 问题 → digest 汇报方向变化"]:::phase

    subgraph AF["审计面：四入口（不读代码不查库即可看懂）"]
        direction TB
        A1["views/ 仪表盘——研究现在到哪了"]:::asset
        A2["cycle_report.md——这一轮为什么这么做"]:::asset
        A3["DECISION 可 SQL 查询（含 prompt_version）——哪一步判断偏了"]:::asset
        A4["views/ Git log 与 diff——方向怎么演化"]:::asset
        A5["status_card + live strip——现在正在跑什么（§4.6.6）"]:::asset
    end
    DEC -.-> A3
    AF -.-> H

    RPL["回放保证：上下文包快照（输入）+ 版本化提示词（程序）+ DECISION（输出）<br/>三件套归档 → 任一历史决策可重建完整输入，错误定位到『第几轮的哪次打分』"]:::gate
    A3 -.-> RPL

    classDef phase fill:#dbeafe,stroke:#1d4ed8,color:#1e3a8a
    classDef asset fill:#dcfce7,stroke:#15803d,color:#14532d
    classDef human fill:#ffedd5,stroke:#c2410c,color:#7c2d12
    classDef gate fill:#ede9fe,stroke:#6d28d9,color:#4c1d95
    classDef fail fill:#fee2e2,stroke:#b91c1c,color:#7f1d1d
    classDef term fill:#e5e7eb,stroke:#4b5563,color:#1f2937
    classDef dec fill:#fef9c3,stroke:#a16207,color:#713f12
    classDef orch fill:#f1f5f9,stroke:#475569,color:#0f172a
```

---

## 图 08 · 上下文编译与数据写回闭环（含 validate_artifact 预检环）（§5 §11）

**这张图回答**：长时运行不漂移的根。左半 = 真相之上的派生卡片层与索引（**写路径即索引维护路径，同一事务**）；右半 = 编译器四区包 → Codex 会话 → 产物经**只读预检环 + 确定性三级校验**写回，构成闭环。

<!-- png: 08-上下文编译闭环 -->
```mermaid
flowchart LR
    DB[("db/research.sqlite（WAL）<br/>唯一真相 P1：对象模型 · 状态机<br/>事实表 run / evaluation / metric_result · DECISION")]:::asset

    subgraph DRV["派生卡片层 + 索引（专为上下文供给设计）"]
        direction TB
        CARDS["七类卡片（定额预算 md 片段，物化 DB card 表，携 src_hash 可机械检出过期）<br/>问题卡 ≤200 · 子树摘要 ≤400（subtree_card_depth=1）· 方法族 ≤300 · 基线 ≤250<br/>变体 ≤150（含各 evaluation 摘要 + 反向链接）· 失败 ≤150 · 协议 ≤150"]:::asset
        IDX["索引（建在卡片层与事实表上）<br/>结构（parent_id+依赖边）· 调度（status+综合分+visit_count）<br/>词法（FTS5）· 语义（嵌入向量）· 规格（canonical_key 唯一+tag）<br/>测量（复用判定 O(1)：variant_id+protocol_id+protocol_ver）"]:::asset
    end

    DB == "门禁写路径同一事务：卡片刷新 + 摘要传播 + FTS/嵌入更新" ==> DRV

    COMP["上下文编译器（编排器内置，确定性）<br/>按阶段检索配方组装四区包：<br/>① 固定锚（任务关键集必含，不截断）② 结构邻域（受限血缘/依赖卡）<br/>③ 检索区（词法+语义+新近+状态权重 top-k）④ 引用区（ref 清单 + 取数工具）<br/>降级顺序固定：检索区 top-k 减半 → 邻域裁剪 → 报警人工"]:::orch

    DRV --> COMP
    COMP -- "确定性：同快照+同配方+同预算→字节一致<br/>包哈希+分区 manifest 写 DECISION · 快照归档 cycles/&lt;id&gt;/" --> PACK["context_pack/（只读目录）"]:::orch

    PACK --> CODEX["Codex 会话（Runner 窄接口 run_task）<br/>一任务一会话、会话无记忆（P1/P6）· 无库凭据、无 views/ 写权限<br/>MCP 只读工具：ctx-fetch 按需深潜 · 文献检索 · validate_artifact 预检"]:::phase

    CODEX -. "① 只读预检（实时反馈，不写库）<br/>validate_artifact：自查 schema / 引用完整性" .-> PRECHK["MCP validate_artifact（只读）"]:::gate
    PRECHK -. "反馈问题，会话内自改" .-> CODEX

    CODEX -- "产物（schema JSON + md 正文）落 artifacts/ staging，原子改名提交" --> VAL["② 编排器三级校验（事务内重校验，确定性仲裁）<br/>schema → 引用完整性 → 业务门禁（预检结论不被信任，真相侧再验）"]:::gate
    VAL -- "任一级失败：记 DECISION(actor=gate, reject) 重试 ≤2 → 升级阶段失败" --> CODEX
    VAL -- "通过" --> GW["门禁函数 gate_*（不变量校验）= 资产层唯一写入口"]:::gate
    GW == "写回真相，同一事务刷新卡片与索引（闭环）" ==> DB

    classDef phase fill:#dbeafe,stroke:#1d4ed8,color:#1e3a8a
    classDef asset fill:#dcfce7,stroke:#15803d,color:#14532d
    classDef human fill:#ffedd5,stroke:#c2410c,color:#7c2d12
    classDef gate fill:#ede9fe,stroke:#6d28d9,color:#4c1d95
    classDef fail fill:#fee2e2,stroke:#b91c1c,color:#7f1d1d
    classDef term fill:#e5e7eb,stroke:#4b5563,color:#1f2937
    classDef dec fill:#fef9c3,stroke:#a16207,color:#713f12
    classDef orch fill:#f1f5f9,stroke:#475569,color:#0f172a
```

---

## 图 09 · 长时运行：恢复 · watchdog · 预算 · 停机（§6 §8）

**这张图回答**：数百轮无人值守怎么不挂、不烧穿预算、何时停。三套机制：**阶段边界即检查点**（步进函数幂等，kill -9 安全）；**watchdog 绝不无限重试**；**预算台账一处记账三处消费**。会话双模式 A/B 在 M6 实测定默认。

<!-- png: 09-长时运行 -->
```mermaid
flowchart TD
    CRON["运行形态（§8.1）：cron / 循环守护 + advance 步进<br/>每次唤醒推进一格 → 落库 → 退出（与幂等恢复天然契合）<br/>会话双模式：A 一 turn 一阶段 / B 一 turn 跨多阶段 Codex 自决何时停"]:::orch
    CRON --> IDEM{"幂等检查：该阶段产物与 DECISION 已存在？"}:::dec
    IDEM -- "存在：跳过该步" --> NXT["推进到下一阶段"]:::orch
    IDEM -- "不存在：执行" --> EXEC["执行阶段步进函数<br/>半成品只写 staging → 完成后原子改名"]:::phase
    EXEC --> CKPT["落库（阶段边界即检查点 §8.4）"]:::asset
    NXT --> CRON
    CKPT --> CRON

    KILL["⚡ 任意时刻 kill -9 / 断电 / 崩溃"]:::fail
    KILL -.-> RCV["重启：编排器读在途 CYCLE 状态字段从该阶段继续<br/>（P1 全状态在库）· 验收 M3：杀进程重启续跑终态与不杀一致"]:::orch
    RCV -.-> CRON

    subgraph WDG["watchdog 与挂死处理（§8.2）"]
        direction TB
        W1["每次 Runner 调用带 wall-clock 上限（探针级 2h / 全量级默认 24h）<br/>transcript mtime 作基础心跳（综合判活 二部§6.7）"]:::orch
        W1 -- "超时" --> W2["终止会话 → 写 failed → 按重试策略<br/>（smoke 修复受预算上限 / 产物解析 ≤2 / plan 评审 ≤2 / bundle 双评审各 ≤3）"]:::fail
        W2 -- "连续同型失败超上限" --> W3["轮次失败 + 通知人类——绝不无限重试"]:::fail
    end
    WDG -.-> CKPT

    subgraph BUD["预算台账（§8.3）"]
        direction TB
        L1["每次 Runner 调用与每个 run/evaluation 的消耗记入 LEDGER"]:::asset
        L1 --> L2["按轮聚合到 CYCLE.cost_total"]:::asset
        L2 --> L3["三处消费：① 采集成本项 cost_norm<br/>② 单轮预算 B(t)：初值≈廉价探针，每 m 轮 ×2 封顶 B_max<br/>③ 全局预算耗尽判停（上限改动只能人类 set_budget）"]:::orch
    end
    CKPT -.-> L1

    L3 --> STOPQ{"终止三判据（任一触发即出环）"}:::dec
    STOPQ -- "① 全 open 问题 score < τ 连续若干轮" --> STOP
    STOPQ -- "② 全局预算耗尽" --> STOP
    STOPQ -- "③ 目标谓词满足（reasoning 判定 + 人类确认）" --> STOP
    STOPQ -- "未触发：继续循环" --> CRON
    STOP(["全局停机：问题树 + baseline 池 + 视图与决策账<br/>即最终可复查资产（任何结论都能回溯到一次具体 evaluation 测量）"]):::term

    subgraph STG["存储治理与时间线（§8）"]
        S1["SQLite WAL：每轮收尾后滚动备份 · views/ 独立 Git 仓每轮一提交（git log 即研究时间线）<br/>档案分级：metrics 与产物 JSON 永存 · 原始日志压缩归档 · worktree 注册后回收<br/>schema 迁移 db/migrations/（升级前自动备份）· 提示词版本号记入 DECISION"]:::asset
    end
    CKPT -.-> STG
    W3 -.-> NOTI["通知钩子（webhook / QQ）：轮次失败 · engineering_blocked · 每 K 轮摘要 · 文件请求三事件"]:::human

    classDef phase fill:#dbeafe,stroke:#1d4ed8,color:#1e3a8a
    classDef asset fill:#dcfce7,stroke:#15803d,color:#14532d
    classDef human fill:#ffedd5,stroke:#c2410c,color:#7c2d12
    classDef gate fill:#ede9fe,stroke:#6d28d9,color:#4c1d95
    classDef fail fill:#fee2e2,stroke:#b91c1c,color:#7f1d1d
    classDef term fill:#e5e7eb,stroke:#4b5563,color:#1f2937
    classDef dec fill:#fef9c3,stroke:#a16207,color:#713f12
    classDef orch fill:#f1f5f9,stroke:#475569,color:#0f172a
```

---

*生成于 2026-06-17 · 源文档：《meta-research 元循环系统 · 施工说明书 v2.2》· 本文件为唯一图源，HTML 与 PNG 均由此渲染。*
